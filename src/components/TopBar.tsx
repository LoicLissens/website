import * as React from "react";

const TopBar = (): JSX.Element => {
  return (
    <nav className="nav-header">
      <a href="#">
        <h1 className="logo-top">Loïc Lissens</h1>
      </a>
    </nav>
  );
};
export default TopBar;
